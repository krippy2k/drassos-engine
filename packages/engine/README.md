# @drassos/engine

Internal Node runtime implementation used by `@drassos/node`.

Application code should import `@drassos/core`, `@drassos/node`, and `@drassos/testing`. This package remains available so existing Drassos examples and the CLI keep working.

Requires **Node.js 20+**.
