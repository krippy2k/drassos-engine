This directory is an isolated consumer fixture. It is not part of the pnpm workspace.
`pnpm smoke:pack` copies `src/` into a temporary project, installs packed tarballs, compiles, and runs a workflow.
