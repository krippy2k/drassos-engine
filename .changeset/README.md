# Changesets

Record package changes with:

```bash
pnpm changeset
pnpm changeset version
pnpm changeset publish
```

Official SDK packages (`@drassos/core`, `@drassos/node`, `@drassos/testing`) stay on the same version. `@drassos/engine` is the implementation package consumed by `@drassos/node`.
